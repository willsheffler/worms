name: wormsnewnumba
channels:
 - schrodinger
 - conda-forge
 - https://pose:foldtree@conda.graylab.jhu.edu
dependencies:
 - python >= 3.7
 - pyrosetta
 - numpy>=1.16.3
 - matplotlib
 - scipy
 - numba
 - pandas>=0.24.2
 - xarray>=0.11.3
 - pytest
 - pytest-faulthandler
 - sphinx_rtd_theme
 - ipython
 - cffi
 - pymol >= 2.1
 - python-blosc >= 1.5
 - psutil
 - pympler
 - pip
 - pip:
   - homog >= 0.1.10
   - xbin >= 0.1.5
   - hypothesis
   - pytest-sugar
   - pytest-xdist
   - tqdm
   - yapf
